#include "benchmark/benchmark.h"
#include "utility.h"

namespace benchmark {

JSON CompareResults(JSON const& R1, JSON const& R2) {
  ((void)R1);
  ((void)R2);
  JSON res;
  // FIXME
  return res;
}

}  // namespace benchmark
